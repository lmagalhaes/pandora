build:
  nodes:
    coverage:
      environment:
        python: 3.11.9
      tests:
        override:
          - command: 'pip install pipenv && pipenv install -d && pytest'
            coverage:
              file: '.coverage'
              format: 'py-cc'
